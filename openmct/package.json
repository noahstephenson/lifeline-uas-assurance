{
  "name": "lifeline-openmct",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8766",
    "test": "node --test",
    "test:e2e": "playwright test",
    "test:qualification": "playwright test tests/display-qualification.spec.js",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 8766"
  },
  "dependencies": {
    "openmct": "4.1.0"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1",
    "vite": "^6.1.0"
  },
  "engines": {
    "node": ">=20 <21"
  }
}
