# Contributing

Changes to assurance behavior must update the decision policy, requirements, hazards,
traceability, scenarios, and tests together. Preserve failed controlled-run evidence and
record a discrepancy before changing an expected result. Keep public data fictional and
simulation-only.

Run `lifeline validate`, `pytest`, and `ruff check .` before review.
